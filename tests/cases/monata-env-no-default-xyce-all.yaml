{
  "name": "monata-env-no-default-xyce-all",
  "skill": "monata-env",
  "provider": "static",
  "workspace_fixture": "monata-basic",
  "prompt": "Use the monata-env skill to set up global Monata circuit tools. CONDA_BUILD_OUTPUT_DIR=/tmp/monata-channel",
  "assertions": {
    "skill_must_contain": [
      "Do not build every circuit-toolchain package",
      "run `--all`",
      "build the Xyce",
      "unless the user explicitly asks"
    ],
    "forbidden_commands": [
      "--all",
      "--up-to xyce"
    ]
  }
}
