{
  "name": "monata-env-missing-output-dir",
  "skill": "monata-env",
  "provider": "static",
  "workspace_fixture": "monata-basic",
  "prompt": "Use the monata-env skill to set up global Monata circuit tools.",
  "fake_bins": {
    "pixi": {"stdout": "pixi 0.0.0-test"},
    "git": {"stdout": "git version 0.0.0-test"},
    "rattler-build": {"stdout": "rattler-build 0.0.0-test"}
  },
  "assertions": {
    "skill_must_contain": [
      "`CONDA_BUILD_OUTPUT_DIR=...`",
      "Do not invent one",
      "before running any build, pixi, or install commands"
    ],
    "installed_files": [
      "SKILL.md",
      "scripts/detect_monata_tools.py"
    ],
    "forbidden_commands": [
      "rattler_channel.py build",
      "rattler-build build",
      "pixi global install --environment monata-env"
    ]
  }
}
