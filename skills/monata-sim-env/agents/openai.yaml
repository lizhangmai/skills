interface:
  display_name: "Monata Sim Env"
  short_description: "Set up Monata simulation environments"
  default_prompt: "Use $monata-sim-env to set up this Monata environment. Inspect the Monata workspace to choose required tools. If I did not provide CONDA_BUILD_OUTPUT_DIR explicitly, ask me before running build, pixi, or install commands."
