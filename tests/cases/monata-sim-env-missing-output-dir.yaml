{
  "name": "monata-sim-env-missing-output-dir",
  "skill": "monata-sim-env",
  "provider": "static",
  "workspace_fixture": "monata-basic",
  "prompt": "Use the monata-sim-env skill to set up this complete Monata environment and run the README demo.",
  "fake_bins": {
    "pixi": {"stdout": "pixi 0.0.0-test"},
    "git": {"stdout": "git version 0.0.0-test"},
    "rattler-build": {"stdout": "rattler-build 0.0.0-test"}
  },
  "assertions": {
    "skill_must_contain": [
      "`CONDA_BUILD_OUTPUT_DIR=...`",
      "Do not invent one",
      "before running any build, pixi, or install commands"
    ],
    "installed_files": [
      "SKILL.md",
      "scripts/detect_monata_tools.py"
    ],
    "forbidden_commands": [
      "pixi init",
      "pixi add",
      "rattler_channel.py build",
      "rattler-build build"
    ]
  }
}
